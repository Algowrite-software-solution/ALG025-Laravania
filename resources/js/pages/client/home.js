import HomeManager from "../../UI/HomeManager";

// Our Collection Section lists autoplay animation & functionality of arrow buttons
document.addEventListener("DOMContentLoaded", function () {
    const homeManager = new HomeManager();
});
