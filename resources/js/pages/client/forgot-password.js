import ForgotPasswordManager from "../../UI/ForgotPasswordManager";

let forgotPasswordManager = null;
document.addEventListener("DOMContentLoaded", () => {
    forgotPasswordManager = new ForgotPasswordManager();
});